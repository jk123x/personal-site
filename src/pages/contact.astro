---
// src/pages/contact.astro
---

<!DOCTYPE html>
<html lang="en-AU">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact | Jay Kinkade</title>
    <meta name="description" content="Get in touch with Jay Kinkade in Melbourne.">
    <link rel="canonical" href="https://www.jaykinkade.com/contact">

    <!-- Geo tags -->
    <meta name="geo.region" content="AU-VIC">
    <meta name="geo.placename" content="Melbourne">

    <!-- Open Graph -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="Contact | Jay Kinkade">
    <meta property="og:description" content="Get in touch with Jay Kinkade in Melbourne.">
    <meta property="og:url" content="https://www.jaykinkade.com/contact">
    <meta property="og:locale" content="en_AU">
    <meta property="og:site_name" content="Jay Kinkade">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="Contact | Jay Kinkade">
    <meta name="twitter:description" content="Get in touch with Jay Kinkade in Melbourne.">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="nav">
        <a href="/" class="nav-logo">JK</a>
        <div class="nav-links">
            <a href="/about">About</a>
            <a href="/contact" class="active">Contact</a>
        </div>
    </nav>

    <main class="main">
        <div class="content-wrapper">
            <p class="email" id="email">jay.kinkade@gmail.com</p>
            <p class="hint">(click to copy)</p>
        </div>
    </main>

    <script>
        const email = document.getElementById('email');

        email.addEventListener('click', async () => {
            await navigator.clipboard.writeText('jay.kinkade@gmail.com');
            email.classList.add('copied');
            setTimeout(() => email.classList.remove('copied'), 300);
        });
    </script>
</body>
</html>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        margin: 0;
        padding: 0;
        height: 100%;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        color: #2c2825;
        background: #faf9f7;
        -webkit-font-smoothing: antialiased;
        height: 100vh;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 4rem;
        max-width: calc(850px + 8rem);
        margin: 0 auto;
        width: 100%;
    }

    .nav-logo {
        font-size: 0.875rem;
        font-weight: 500;
        color: #a08060;
        text-decoration: none;
        letter-spacing: 0.02em;
    }

    .nav-links {
        display: flex;
        gap: 2rem;
    }

    .nav-links a {
        color: #857c72;
        text-decoration: none;
        font-size: 0.813rem;
        transition: color 0.2s ease;
    }

    .nav-links a:hover,
    .nav-links a.active {
        color: #2c2825;
    }

    .main {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem 4rem;
    }

    .content-wrapper {
        text-align: center;
    }

    .email {
        font-size: 1.125rem;
        color: #857c72;
        font-weight: 300;
        animation: fadeUp 0.8s ease-out both;
        cursor: pointer;
        transition: color 0.3s ease;
    }

    .email:hover {
        color: #2c2825;
    }

    .hint {
        font-size: 0.7rem;
        color: #b5aea5;
        margin-top: 0.75rem;
        opacity: 0;
        transition: opacity 0.2s ease;
    }

    .content-wrapper:hover .hint {
        opacity: 1;
    }

    .email.copied {
        animation: pulse 0.3s ease;
    }

    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }


    @media (max-width: 768px) {
        .nav {
            padding: 1.5rem;
        }

        .main {
            padding: 2rem 1.5rem;
        }

        .hint {
            opacity: 1;
        }
    }
</style>
